# These are optional overrides for the defaults baked into application.properties.
# Spring Boot does not load .env files automatically -- export these as real
# environment variables (or pass -D system properties) if you want to override
# the local-dev defaults, e.g. for a non-local database or a production JWT secret.

DB_URL=jdbc:mysql://localhost:3306/task_tracker_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
DB_USERNAME=root
DB_PASSWORD=1234

JWT_SECRET=K56ucT7KZGPNei736Q3+w/Vfm5z40gy6Xsump2PV/EM=
JWT_EXPIRATION=86400000

ADMIN_USERNAME=admin
ADMIN_EMAIL=admin@tasktracker.local
ADMIN_PASSWORD=admin123

CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

SERVER_PORT=8080
